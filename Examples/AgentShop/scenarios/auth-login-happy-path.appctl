# Password login with a seeded account saves the session and goes home.
expect screen=auth/login canSubmit=false
email alice@example.com
expect canSubmit=false
password Passw0rd!
expect canSubmit=true revealed=none
show-password on
expect revealed=password
submit
expect screen=home/orders orders=3 call=auth.login call=session.save call=orders.fetchOrders
