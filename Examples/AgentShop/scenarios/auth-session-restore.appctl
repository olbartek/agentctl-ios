# A relaunch restores the saved session; the mock backend keeps its data.
login-as alice
open 1003
cancel
expect status=cancelled
reset
expect screen=home/orders call=session.current call=orders.fetchOrders statuses=delivered,shipped,cancelled
