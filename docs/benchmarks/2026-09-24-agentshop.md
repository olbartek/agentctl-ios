# AgentShop benchmark: headless vs simulator vs XCUITest (2026-09-24)

Generated by `python3 Examples/AgentShop/bench/bench.py`. Every row runs the **same scenario file** in each
mode: the headless CLI, the real app on a simulator driven through AgentCtlBridge, and an XCUITest generated
from the scenario (`bench/gen_uitests.py`), which taps and types through the real UI.

- **Machine:** Apple M4 Max, 14 cores, macOS 26.7
- **Tools:** Xcode 26.6 Build version 17F113; Apple Swift version 6.3.3 (swiftlang-6.3.3.1.3 clang-2100.1.1.101)
- **Simulator:** iPhone 17 Pro (iOS 27.0, 19E6233E-D28A-429C-8857-E234D031A664)
- **Runs:** headless 3× per scenario (median); bridge and UI tests once each.
- **Settings:** both simulator modes launch a fresh app with no saved session and no mock latency, so the numbers compare the harnesses, not the fake backend.

**Video:** [three scenarios in all three modes, side by side](2026-09-24-agentshop.mp4).

## Summary

- **99 scenarios, 1104 steps, three ways.** Headless: **1.3 s**. The real app on a simulator, through the bridge: **8 min 2 s**. The generated XCUITests: **30 min 8 s**.
- The UI tests take **1,441×** as long as the headless run, and **4×** as long as the same scenarios driven through the bridge on the same simulator.
- After a one-line change to a reducer, verifying it headlessly (build + scenario) takes **1.6 s**; through a UI test, **35.2 s** (22×).
- Headless runs no simulator and renders no views, so it checks the app's logic and navigation, not pixels. The bridge checks the same things in the real app on a device, and the XCUITests check that the UI shows and reaches them.
  The three are complementary; the point is which one an agent should run hundreds of times a day.

## The three modes

| Mode | What runs | How a step is sent | What it proves | Needs |
|---|---|---|---|---|
| **Headless** (`./appctl test`) | The features' reducers and the mocked clients, in a Mac process | A command, straight to the screen's store | Logic, navigation, errors, calls made, values the screen would show | `swift build` only |
| **Simulator bridge** (`./appctl app run`) | The real app on a simulator, views and all | A command over the app's DEBUG-only HTTP bridge, then a 250 ms quiet wait | The same, in the real app process, with real views rendered | A simulator and a debug build |
| **XCUITest** (generated) | The real app on a simulator, driven from a test runner app | A tap or typing on an accessibility element, then waits for what the screen shows | That the UI can reach every state: elements exist, are on screen and are tappable | A simulator, `build-for-testing`, a test runner |

## Results

### All modes, all scenarios

| Mode | Total | Per scenario | Per step | vs XCUITest |
|---|---|---|---|---|
| Headless, one process for everything | **1.3 s** | 13 ms | 1 ms | 1,441× faster |
| Headless, one process per scenario | **3.0 s** | 30 ms | 3 ms | 610× faster |
| Simulator bridge, relaunch + run | **8 min 2 s** | 4.9 s | 437 ms | 4× faster |
| Simulator bridge, run only | **3 min 51 s** | 2.3 s | 209 ms | 8× faster |
| XCUITest | **30 min 8 s** | 18.3 s | 1.6 s | — |

### Per group

| Group | Scenarios | Steps | Headless, one process | Headless, one process per scenario | Simulator bridge (launch + run) | XCUITest | XCUITest vs headless |
|---|---|---|---|---|---|---|---|
| Authentication | 29 | 346 | **316 ms** | 708 ms | 2 min 16 s (63.5 s running) | **9 min 38 s** | 1,829× |
| Onboarding | 26 | 297 | **385 ms** | 1.1 s | 2 min 16 s (66.0 s running) | **6 min 49 s** | 1,061× |
| Shop | 44 | 461 | **554 ms** | 1.2 s | 3 min 31 s (101.1 s running) | **13 min 41 s** | 1,483× |
| **All** | 99 | 1104 | **1.3 s** | 3.0 s | 8 min 2 s (3 min 51 s running) | **30 min 8 s** | 1,441× |

- *Headless, one process* is `shopctl test` over the whole group: how an agent (or CI) checks everything.
- *One process per scenario* adds a process start (~20–40 ms) to every scenario.
- *Simulator bridge* relaunches the app for every scenario, as a UI test does; *running* is the scenario alone.
- *XCUITest* is the wall time of `xcodebuild test-without-building` for the group (already built).

### The loop an agent runs: change a line, verify it

A one-line change to the `ShopFeed` reducer, then `shop-filter-category` verified (median of 3).

| | Time |
|---|---|
| Headless: `swift build` of the CLI + the scenario | **1.6 s** |
| UI test: `xcodebuild build-for-testing` + the scenario's UI test | **35.2 s** |
| Ratio | 22× |

### One-off costs

| | Time |
|---|---|
| `swift build` of the CLI (warm) | 563 ms |
| `xcodebuild build-for-testing` (warm) | 12.7 s |

## Why headless is the fastest, and where the other two spend their time

Each mode's time per scenario, fitted as *a fixed cost per scenario + a cost per step* over every scenario in this run (a step is one line of a scenario: a command or an `expect`):

| Mode | Fixed cost per scenario | Cost per step | A 12-step scenario |
|---|---|---|---|
| Headless | 4 ms | 1 ms | **18 ms** |
| Simulator bridge | 2.5 s | 211 ms | **5.1 s** |
| XCUITest | 5.1 s | 1.1 s | **18.4 s** |

### Headless: nothing to wait for

- **No simulator, no app, no views.** The scenario runs in a Mac process that holds the app's features (their reducers and state) with the mocked clients. Nothing is installed, launched or rendered.
- **A command goes straight to the screen's store.** There is no touch to synthesize and no element to find: `add-to-cart` is the action the button would send.
- **Time is virtual.** The app's clock is a test clock, so a mock's latency, a debounce or `advance 30s` take no real time. A step is settled the moment its effects have finished, not after a wait.
- What is left is the Swift work itself: 1 ms a step. Starting the process is the biggest cost, and one `shopctl test` pays it once for every scenario.

### Simulator bridge: a real app, and real time

- **A launch per scenario** (2.5 s on average): `simctl` terminates and relaunches the app with a clean session, the app starts, and its bridge must answer before the first step. (Each scenario starts fresh, as a UI test does, so the comparison is fair; one launch could run many.)
- **A settle window per step** (211 ms a step here): the command is sent over HTTP and applied on the app's main thread, like the headless one, but the app's effects run on real time. So the bridge can only call a step settled once no mocked call is in flight and the state has stayed unchanged for **250 ms**. Every command pays that quiet window; an `expect` sends nothing and costs almost nothing, which is why the average step comes out below it. The window is the price of knowing a step is finished without a virtual clock.
- **Real rendering.** SwiftUI lays out and draws every screen, with its animations. The bridge does not wait for them, but they share the main thread with the app.

### XCUITest: a second process, touching the screen

- **A launch per test** (`test_launch`, which only launches and checks one screen, takes 5.2 s): XCTest terminates the app, launches it with the test's arguments, and waits for it to be idle before the first query.
- **Every step goes through the UI** (1.1 s a step here). The test runs in a separate runner app that sees the app only through accessibility. To tap a button it asks the app for a snapshot of its element tree, finds the element, waits for the app to be idle (no animation running), synthesizes the touch and waits for idle again. Each of those is a round trip between two processes.
- **Typing is key by key** on the software keyboard, and the keyboard animates in and out.
- **Navigation animates.** A push, a sheet or a tab switch has to finish before the next element can be tapped.
- **Checks poll.** An expectation is a wait for an element or a value to appear, re-reading the tree until it does. A UI test cannot ask the app what state it is in; it can only look.
- **What every UI suite needs on top:** scrolling elements out from under the keyboard or into view, and closing system prompts ("Save Password?"). Each costs more round trips and animations.

The bridge and the UI tests run the same app on the same simulator. The difference between them is what driving through the UI costs; the difference between the bridge and headless is what a running app and real time cost.

They are not interchangeable, though. The UI tests are the only mode that checks what a user can reach: while building this benchmark they found one place where the headless run disagreed with iOS (selecting the selected tab pops it to its first screen), which the app now does headlessly too. Headless is for the hundreds of checks an agent runs while it works; the UI tests are for what only the real UI can tell you.

## What the UI tests check

The generated UI tests assert 456 of the scenarios' 588 expectations through the UI (screens, errors, shown values, enabled buttons). The other 132 — `loading=`, `call=`, `pending=`, values no screen shows — are checked headlessly only.

## Headless-only scenarios

These move a virtual clock (`advance`) or relaunch (`reset`), which a UI test can only do by really waiting or relaunching. Headlessly they take milliseconds:

| Scenario | Steps | Headless | Real time a UI test would wait |
|---|---|---|---|
| `auth-keep-signed-in` | 15 | 29 ms | — |
| `auth-otp-code-expired` | 14 | 37 ms | 5 min 0 s |
| `auth-otp-resend-countdown` | 13 | 31 ms | 30.0 s |
| `auth-otp-wrong-code-3x` | 21 | 41 ms | 30.0 s |
| `auth-register-verify-errors` | 35 | 59 ms | 5 min 30 s |
| `auth-session-restore` | 10 | 28 ms | — |

## Every scenario

<details><summary>Authentication (29 scenarios)</summary>

| Scenario | Steps | Headless (in-process) | Headless (with process start) | Bridge launch | Bridge run | XCUITest |
|---|---|---|---|---|---|---|
| `auth-forgot-back-navigation` | 9 | 9 ms | 21 ms | 2.9 s | 1.3 s | 11.0 s |
| `auth-forgot-password` | 25 | 25 ms | 37 ms | 2.4 s | 4.4 s | 29.4 s |
| `auth-forgot-password-mismatch` | 12 | 12 ms | 25 ms | 2.5 s | 2.1 s | 17.9 s |
| `auth-forgot-unknown-email` | 9 | 11 ms | 23 ms | 2.4 s | 1.9 s | 15.4 s |
| `auth-google-network-error` | 5 | 7 ms | 18 ms | 2.5 s | 591 ms | 7.3 s |
| `auth-google-sign-in` | 23 | 28 ms | 39 ms | 2.3 s | 4.5 s | 24.2 s |
| `auth-launch` | 1 | 2 ms | 13 ms | 2.4 s | 41 ms ✗ | 5.2 s |
| `auth-lockout-then-reset` | 19 | 23 ms | 34 ms | 2.4 s | 4.4 s | 34.5 s ✗ |
| `auth-login-happy-path` | 9 | 10 ms | 21 ms | 2.4 s | 1.2 s | 11.5 s |
| `auth-login-locked` | 17 | 15 ms | 27 ms | 2.3 s | 3.3 s | 24.8 s |
| `auth-login-network-error-retry` | 7 | 10 ms | 22 ms | 2.5 s | 1.2 s | 11.7 s |
| `auth-login-show-password` | 9 | 10 ms | 21 ms | 2.3 s | 1.4 s | 13.8 s |
| `auth-login-submit-disabled` | 10 | 7 ms | 19 ms | 2.3 s | 1.3 s | 19.6 s |
| `auth-login-unknown-email` | 6 | 7 ms | 18 ms | 2.3 s | 1.1 s | 12.5 s |
| `auth-login-wrong-password` | 11 | 12 ms | 23 ms | 2.2 s | 1.8 s | 15.8 s |
| `auth-logout` | 10 | 14 ms | 25 ms | 2.3 s | 1.8 s | 15.6 s |
| `auth-logout-cancel` | 10 | 15 ms | 26 ms | 2.3 s | 2.0 s | 19.3 s |
| `auth-otp-back-to-login` | 10 | 10 ms | 20 ms | 2.8 s | 1.6 s | 11.4 s |
| `auth-otp-happy-path` | 9 | 12 ms | 22 ms | 2.6 s | 1.5 s | 13.9 s |
| `auth-otp-unknown-email` | 8 | 9 ms | 20 ms | 2.7 s | 1.4 s | 17.9 s |
| `auth-otp-wrong-code` | 10 | 14 ms | 25 ms | 2.5 s | 2.3 s | 17.8 s |
| `auth-register-back-to-login` | 8 | 8 ms | 18 ms | 2.6 s | 1.4 s | 12.8 s |
| `auth-register-email-taken` | 12 | 14 ms | 25 ms | 2.5 s | 2.4 s | 20.4 s |
| `auth-register-happy-path` | 21 | 24 ms | 35 ms | 2.4 s | 4.4 s | 26.7 s |
| `auth-register-password-mismatch` | 11 | 10 ms | 21 ms | 2.6 s | 2.1 s | 21.1 s |
| `auth-register-terms-required` | 10 | 10 ms | 20 ms | 2.4 s | 1.8 s | 18.1 s |
| `auth-register-validation` | 27 | 19 ms | 29 ms | 2.6 s | 4.2 s | 39.8 s |
| `auth-register-verify-wrong-code` | 15 | 17 ms | 28 ms | 2.5 s | 3.2 s | 26.0 s |
| `auth-sign-in-again-after-logout` | 13 | 20 ms | 31 ms | 3.4 s | 2.9 s | 27.1 s ✗ |

</details>

<details><summary>Onboarding (26 scenarios)</summary>

| Scenario | Steps | Headless (in-process) | Headless (with process start) | Bridge launch | Bridge run | XCUITest |
|---|---|---|---|---|---|---|
| `onboarding-address-back-keeps-interests` | 12 | 21 ms | 39 ms | 3.3 s | 2.4 s | 13.7 s |
| `onboarding-address-continue-disabled` | 14 | 22 ms | 41 ms | 2.5 s | 2.9 s | 18.7 s |
| `onboarding-address-fix-zip` | 17 | 25 ms | 46 ms | 2.8 s | 3.7 s | 22.8 s |
| `onboarding-address-invalid-zip` | 13 | 24 ms | 42 ms | 3.3 s | 3.2 s | 19.7 s |
| `onboarding-address-prefills-checkout` | 18 | 42 ms | 60 ms | 4.0 s | 4.7 s | 25.8 s |
| `onboarding-address-skip` | 10 | 18 ms | 35 ms | 3.7 s | 2.4 s | 14.5 s |
| `onboarding-address-valid` | 14 | 22 ms | 38 ms | 2.6 s | 3.3 s | 19.9 s |
| `onboarding-after-google` | 11 | 20 ms | 36 ms | 2.6 s | 2.5 s | 10.3 s |
| `onboarding-after-register` | 21 | 33 ms | 47 ms | 3.7 s | 5.1 s | 28.9 s |
| `onboarding-back-from-notifications` | 15 | 17 ms | 30 ms | 3.2 s | 3.4 s | 20.0 s |
| `onboarding-complete-lands-on-shop` | 10 | 16 ms | 28 ms | 2.8 s | 2.5 s | 13.9 s |
| `onboarding-complete-network-error-retry` | 13 | 18 ms | 38 ms | 2.4 s | 2.8 s | 14.2 s |
| `onboarding-interests-back` | 9 | 16 ms | 38 ms | 2.3 s | 1.9 s | 11.9 s |
| `onboarding-interests-continue-disabled` | 9 | 16 ms | 36 ms | 2.3 s | 1.6 s | 11.7 s |
| `onboarding-interests-select-two` | 9 | 20 ms | 40 ms | 2.3 s | 1.9 s | 12.3 s |
| `onboarding-interests-toggle-off` | 10 | 22 ms | 43 ms | 2.3 s | 2.1 s | 12.9 s |
| `onboarding-interests-too-many` | 15 | 29 ms | 50 ms | 2.3 s | 2.9 s | 14.8 s |
| `onboarding-notifications-allow` | 10 | 31 ms | 53 ms | 2.3 s | 2.5 s | 13.6 s |
| `onboarding-notifications-not-now` | 10 | 33 ms | 56 ms | 2.4 s | 2.5 s | 13.4 s |
| `onboarding-onboarded-user-skips` | 4 | 18 ms | 39 ms | 2.5 s | 931 ms | 9.9 s |
| `onboarding-once-per-account` | 17 | 55 ms | 75 ms | 2.4 s | 4.3 s | 23.7 s |
| `onboarding-profile-error-goes-home` | 5 | 17 ms | 37 ms | 2.4 s | 945 ms | 9.8 s |
| `onboarding-starts-after-first-login` | 4 | 10 ms | 29 ms | 2.3 s | 825 ms | 9.9 s |
| `onboarding-welcome-back` | 10 | 15 ms | 33 ms | 2.4 s | 1.9 s | 12.2 s |
| `onboarding-welcome-pages` | 10 | 14 ms | 31 ms | 2.4 s | 1.6 s | 11.7 s |
| `onboarding-welcome-skip` | 7 | 13 ms | 31 ms | 2.4 s | 1.3 s | 11.2 s |

</details>

<details><summary>Shop (44 scenarios)</summary>

| Scenario | Steps | Headless (in-process) | Headless (with process start) | Bridge launch | Bridge run | XCUITest |
|---|---|---|---|---|---|---|
| `shop-add-same-line-twice` | 10 | 15 ms | 26 ms | 2.5 s | 2.3 s | 18.5 s |
| `shop-add-to-cart` | 8 | 12 ms | 25 ms | 2.4 s | 1.7 s | 14.3 s |
| `shop-add-two-sizes` | 12 | 17 ms | 28 ms | 2.3 s | 2.8 s | 18.0 s |
| `shop-cart-dec-removes-line` | 8 | 13 ms | 24 ms | 2.3 s | 2.0 s | 14.9 s |
| `shop-cart-empty-checkout-disabled` | 5 | 9 ms | 20 ms | 2.4 s | 1.2 s | 12.9 s |
| `shop-cart-inc-dec` | 11 | 16 ms | 27 ms | 2.4 s | 2.5 s | 16.6 s |
| `shop-cart-remove` | 13 | 19 ms | 30 ms | 2.4 s | 3.0 s | 21.3 s |
| `shop-checkout-apple-pay` | 11 | 19 ms | 31 ms | 2.3 s | 2.6 s | 18.2 s |
| `shop-checkout-back-to-cart` | 9 | 16 ms | 29 ms | 2.4 s | 2.3 s | 16.3 s |
| `shop-checkout-express-shipping` | 12 | 17 ms | 29 ms | 2.4 s | 2.6 s | 17.9 s |
| `shop-checkout-happy-path` | 19 | 26 ms | 38 ms | 2.3 s | 4.5 s | 29.5 s |
| `shop-checkout-invalid-card` | 13 | 20 ms | 33 ms | 2.4 s | 3.1 s | 22.5 s |
| `shop-checkout-invalid-zip` | 13 | 19 ms | 33 ms | 2.4 s | 3.1 s | 24.3 s |
| `shop-checkout-network-error-retry` | 13 | 21 ms | 34 ms | 2.5 s | 2.9 s | 19.9 s |
| `shop-checkout-payment-declined` | 13 | 23 ms | 34 ms | 2.3 s | 3.1 s | 20.9 s |
| `shop-checkout-prefilled-address` | 10 | 17 ms | 30 ms | 2.4 s | 2.3 s | 18.1 s |
| `shop-checkout-validation` | 17 | 23 ms | 35 ms | 2.4 s | 3.6 s | 28.5 s |
| `shop-confirmation-continue-shopping` | 13 | 22 ms | 33 ms | 2.4 s | 3.1 s | 22.7 s |
| `shop-confirmation-view-order` | 13 | 24 ms | 35 ms | 2.4 s | 3.3 s | 21.6 s |
| `shop-feed-loads-after-login` | 4 | 9 ms | 21 ms | 2.4 s | 951 ms | 10.2 s |
| `shop-feed-network-error-retry` | 7 | 10 ms | 23 ms | 2.4 s | 1.2 s | 11.1 s |
| `shop-feed-refresh-error-keeps-products` | 8 | 11 ms | 22 ms | 2.4 s | 1.5 s | 13.6 s |
| `shop-filter-all-resets` | 7 | 10 ms | 22 ms | 2.4 s | 1.5 s | 16.9 s |
| `shop-filter-category` | 9 | 11 ms | 23 ms | 2.4 s | 1.7 s | 13.6 s |
| `shop-new-order-appears-in-orders` | 15 | 24 ms | 36 ms | 2.3 s | 3.5 s | 27.4 s |
| `shop-open-product` | 7 | 12 ms | 22 ms | 2.4 s | 1.5 s | 13.7 s |
| `shop-order-cancel` | 12 | 19 ms | 31 ms | 2.3 s | 2.5 s | 18.2 s |
| `shop-order-detail-errors` | 14 | 20 ms | 30 ms | 2.4 s | 2.8 s | 18.7 s |
| `shop-orders-empty` | 7 | 12 ms | 22 ms | 2.8 s | 1.5 s | 13.5 s |
| `shop-orders-error-retry` | 11 | 13 ms | 23 ms | 2.6 s | 1.8 s | 15.3 s |
| `shop-orders-list` | 9 | 15 ms | 26 ms | 2.5 s | 1.8 s | 15.1 s |
| `shop-product-favorite` | 9 | 11 ms | 22 ms | 3.3 s | 1.7 s | 13.3 s |
| `shop-product-out-of-stock` | 5 | 9 ms | 19 ms | 3.3 s | 1.2 s | 13.4 s |
| `shop-product-quantity-limits` | 16 | 15 ms | 26 ms | 2.8 s | 3.0 s | 19.9 s |
| `shop-product-requires-size` | 9 | 11 ms | 22 ms | 2.8 s | 1.8 s | 15.5 s |
| `shop-promo-clear` | 11 | 15 ms | 26 ms | 3.0 s | 2.5 s | 21.1 s |
| `shop-promo-invalid` | 13 | 17 ms | 29 ms | 2.5 s | 2.8 s | 23.5 s |
| `shop-promo-network-error` | 12 | 15 ms | 27 ms | 2.5 s | 2.6 s | 21.9 s |
| `shop-promo-valid` | 10 | 15 ms | 27 ms | 2.5 s | 2.5 s | 14.3 s ✗ |
| `shop-search` | 9 | 11 ms | 22 ms | 2.5 s | 1.7 s | 21.6 s |
| `shop-search-no-results` | 7 | 10 ms | 21 ms | 2.4 s | 1.5 s | 16.7 s |
| `shop-search-within-filter` | 10 | 12 ms | 23 ms | 2.5 s | 2.0 s | 19.3 s |
| `shop-sort-price-asc` | 7 | 10 ms | 21 ms | 2.7 s | 1.5 s | 14.3 s |
| `shop-sort-price-desc` | 10 | 13 ms | 24 ms | 2.4 s | 2.0 s | 16.0 s |

</details>

## Caveats

- One machine, one session. The ratios are more stable than the absolute numbers.
- The bridge settles each step with a 250 ms quiet window, which dominates its per-step time.
- UI tests run one at a time on one simulator; parallel clones would cut the wall time but not the per-test cost.
- The UI tests had to work around what every UI suite hits: the system "Save Password?" sheet, the keyboard covering buttons, and password autofill fighting typed text (`-ui-testing` turns autofill hints off).
- 3 UI tests failed in this run on a bug in the test driver (it sometimes read a keyboard parked below the screen instead of the one covering the button). After the fix they were rerun alone and passed: `AuthUITests/test_lockout_then_reset` 27.5 s, `AuthUITests/test_sign_in_again_after_logout` 22.4 s, `ShopUITests/test_promo_valid` 19.3 s. The group times above are the full run's, failed attempts included.
- Failed: `bridge auth-launch`: its only line checks `call=session.current`, the session lookup at launch. Headlessly the first step reports the launch's calls; through the bridge, `app run` starts after the launch has settled and sees none. The UI test does not check calls, and passes.
